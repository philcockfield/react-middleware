/**
 * For global scripts for the page, such as analytics or tracking codes.
 * For React specific client-side code, use the [views/pages].
 */

if (typeof window !== 'undefined') {
  console.log('Loaded: /scripts (global)');
}
